# Copy this to .env and fill in your real values (the real .env is git-ignored).
# Safe to show on camera — these are placeholders, not real secrets.
AZURE_API_KEY=sk-************************************
AZURE_API_BASE=https://demo.openai.azure.com/
AZURE_API_VERSION=2024-08-01-preview
